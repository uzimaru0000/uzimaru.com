[package]
name = "sleep"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.36"

[package.metadata.component]
package = "uzimaru:sleep"

[package.metadata.component.dependencies]
